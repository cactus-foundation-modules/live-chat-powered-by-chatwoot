// The one name both halves of "open the chat from somewhere else on the page"
// agree on. Its own file so the button that fires it and the loader that answers
// it cannot drift, and so a caller needs nothing else from the widget.
export const LIVE_CHAT_OPEN_EVENT = 'cactus-livechat:open'
