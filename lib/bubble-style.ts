// The chat bubble's colours, shared by the floating bubble and the full-width
// chat button in the shop's slide-out basket - the same control in two places,
// so it wears the same colours in both.
//
// Plain module, no 'use client': see lib/unread.ts.
export const BUBBLE_BG = 'var(--color-accent, #1A5F5A)'
export const BUBBLE_FG = '#fff'
